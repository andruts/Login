<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="Hint" parent="TextAppearance.AppCompat">
        <item name="android:padding">16dp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="Theme.AppCompat.Translucent" parent="Theme.AppCompat.NoActionBar">
            <item name="android:background">#5e000000</item> <!-- Or any transparency or color you need -->
            <item name="android:windowNoTitle">true</item>
            <item name="android:windowBackground">@android:color/transparent</item>
            <item name="android:colorBackgroundCacheHint">@null</item>
            <item name="android:windowIsTranslucent">true</item>
            <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>

    <style name="Theme.EditText" parent="Theme.AppCompat.NoActionBar">
        <item name="android:shape">rectangle</item>
        <item name="android:color">#ffffff</item>
        <item name="android:solidColor">#7e7375</item>
        <item name="android:width">2dp</item>
    </style>

</resources>
